[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node database/migrate.js && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
